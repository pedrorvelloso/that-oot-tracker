import styled from 'styled-components'
import { FiCheck } from 'react-icons/fi'

export const Check = styled(FiCheck).attrs({ size: 18, color: 'green' })`
  position: absolute;
  bottom: 0;
  right: -5px;
`
